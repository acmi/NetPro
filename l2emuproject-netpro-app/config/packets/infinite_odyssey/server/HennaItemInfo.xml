<?xml version="1.0" encoding="UTF-8"?>
<!-- STEALTH PATCH WARNING: this definition is only valid for IO clients updated on 2015-06-10 or later -->

<!-- Stealth patch: the previous error of introducing unsigned CHARs (and allowing character base stats to display +249 and similar modifiers) was fixed by using DWORDs -->
<!-- Periodic dye flag DWORD is back again at the end of packet -->
<packet id="SM_HENNA_ITEM_INFO" xmlns="http://www.l2emu-unique.net" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.l2emu-unique.net ../../../schemata/packet.xsd">
	<dword alias="Henna" type="Henna" />
	<dword alias="Actual item" type="Item" />
	<qword alias="Amount in inventory" />
	<qword alias="Price of appliance" />
	<dword alias="Applicable" type="YesOrNo" />
	<qword alias="Viewer's adena" />
	<dword alias="Current INT" />
	<dword alias="INT with henna" />
	<!-- byte alias="INT with henna" /-->
	<dword alias="Current STR" />
	<dword alias="STR with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="STR with henna" /-->
	<dword alias="Current CON" />
	<dword alias="CON with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="CON with henna" /-->
	<dword alias="Current MEN" />
	<dword alias="MEN with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="MEN with henna" /-->
	<dword alias="Current DEX" />
	<dword alias="DEX with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="DEX with henna" /-->
	<dword alias="Current WIT" />
	<dword alias="WIT with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="WIT with henna" /-->
	<dword alias="Current LUC" />
	<dword alias="LUC with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="LUC with henna" /-->
	<dword alias="Current CHA" />
	<dword alias="CHA with henna" /> <!-- NEW (STEALTH PATCH) -->
	<!-- byte alias="CHA with henna" /-->

	<!-- NEW -->
	<dword alias="Periodic" type="YesOrNo" />
</packet>