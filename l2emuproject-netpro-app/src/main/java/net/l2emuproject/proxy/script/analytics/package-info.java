/**
 * Analytic scripts allow the proxy operator to gain deeper insight into what is (was) going on
 * in-game while reviewing packet logs. These scripts may perform relatively expensive computations,
 * but, as a general rule, do not need to hold the network I/O thread (post-processing).
 */
package net.l2emuproject.proxy.script.analytics;

